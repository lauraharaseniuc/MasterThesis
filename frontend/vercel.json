{
   "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "node scripts/build-activity-allowlist.mjs && npx ng build --configuration production",
  "outputDirectory": "dist/frontend/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
